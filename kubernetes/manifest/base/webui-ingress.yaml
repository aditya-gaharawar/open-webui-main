apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: answer-ai-ingress
  namespace: answer-ai
  #annotations:
    # Use appropriate annotations for your Ingress controller, e.g., for NGINX:
    # nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: answer-ai.minikube.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: answer-ai-service
            port:
              number: 8080
