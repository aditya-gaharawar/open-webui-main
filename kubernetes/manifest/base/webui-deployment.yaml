apiVersion: apps/v1
kind: Deployment
metadata:
  name: answer-ai-deployment
  namespace: answer-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: answer-ai
  template:
    metadata:
      labels:
        app: answer-ai
    spec:
      containers:
      - name: answer-ai
        image: ghcr.io/answer-ai/answer-ai:main
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "500m"
            memory: "500Mi"
          limits:
            cpu: "1000m"
            memory: "1Gi"
        env:
        - name: OLLAMA_BASE_URL
          value: "http://ollama-service.answer-ai.svc.cluster.local:11434"
        tty: true
        volumeMounts:
        - name: webui-volume
          mountPath: /app/backend/data
      volumes:
      - name: webui-volume
        persistentVolumeClaim:
          claimName: answer-ai-pvc          